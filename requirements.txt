# 以下两行：安装本文件时默认走清华 PyPI 镜像（国内加速）。若在境外或镜像异常，可删掉这两行，或执行：
#   pip install -r requirements.txt --index-url https://pypi.org/simple
--index-url https://pypi.tuna.tsinghua.edu.cn/simple
--trusted-host pypi.tuna.tsinghua.edu.cn

# 图片/PDF 转 PPT 流水线依赖
# OCR 使用百度云端高精度含位置版，无需本地 PaddleOCR

# 云端 OCR 与配置
requests>=2.28.0
python-dotenv>=1.0.0

# 去字：渐变拟合（LinearRegression）
scikit-learn>=1.3.0

# 图像与 PDF
Pillow>=9.5.0
numpy>=1.24.0
opencv-python-headless>=4.8.0
PyMuPDF>=1.23.0

# PPT 导出
python-pptx>=0.6.21

# 进度条
tqdm>=4.65.0
