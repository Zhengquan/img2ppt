# =============================================================================
# 图片/PDF 转 PPT - 环境变量示例
# 复制为 .env 后填写：cp .env.example .env
# OCR 使用百度云端「通用文字识别（高精度含位置版）」必填下列两项。
# =============================================================================

# -----------------------------------------------------------------------------
# 百度智能云 - OCR（必填）
# 在百度智能云控制台创建应用，开通「通用文字识别（高精度含位置版）」，
# 获取 API Key 与 Secret Key，需实名认证。有免费额度。
# -----------------------------------------------------------------------------
BAIDU_OCR_API_KEY=your-api-key
BAIDU_OCR_SECRET_KEY=your-secret-key

# -----------------------------------------------------------------------------
# 去字：是否对「复杂纹理」使用 LaMa inpainting（默认不启用，不加载 big-lama）
# 设为 1 或 true 时，会加载 iopaint/LaMa 对复杂区域修补。
# -----------------------------------------------------------------------------
# USE_INPAINTING_FALLBACK=0

# -----------------------------------------------------------------------------
# 其他可选（图像修复等，当前项目未使用）
# -----------------------------------------------------------------------------
# BAIDU_IMAGE_REPAIR_API_KEY=
# BAIDU_IMAGE_REPAIR_SECRET_KEY=
